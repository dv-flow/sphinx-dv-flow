One at a time
=============

`index` already claims these names, so this page describes them without
claiming them again -- which is what `:noindex:` is for.

.. dvf:autoconfig:: ci
   :noindex:

.. dvf:autofilter:: by_arch
   :noindex:
