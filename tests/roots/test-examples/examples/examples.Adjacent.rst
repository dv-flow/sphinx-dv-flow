**Worked example**

This paragraph comes from an adjacent file in the doc set, not from the flow
file. It is included in place, because an example a reader has to click through
to is an example most readers will not read.

.. code-block:: shell

   dfm run examples.Adjacent
